from django.apps import AppConfig


class FinanceLedgerConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.finance_ledger'
    verbose_name = 'Financial Ledger & Audit'
